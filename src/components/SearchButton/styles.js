import styled from "styled-components";

export const Button = styled.button`
    border-radius: 5px;
    border: none;
    width: 40px;
    height: 40px;

    :hover {
        cursor: pointer
    }
`;
